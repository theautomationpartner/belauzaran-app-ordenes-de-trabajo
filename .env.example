# Copiar a `.env.local` y completar. `.env.local` NUNCA se sube al repositorio (ver .gitignore).
#
# Token de la API de Monday de Belaunzaran SA (cuenta 34845072).
VITE_MONDAY_TOKEN=

# Webhook de Make que crea los items en "✋ Orden de Trabajo" (uno por lote).
# Mientras esté vacío, el botón de emitir queda deshabilitado.
VITE_MAKE_WEBHOOK_URL=
