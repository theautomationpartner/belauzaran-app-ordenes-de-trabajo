# Copiar a `.env.local` y completar. `.env.local` NUNCA se sube al repositorio (ver .gitignore).
#
# Token de la API de Monday de Belaunzaran SA (cuenta 34845072). Sólo para desarrollo:
# en el deploy se usa MONDAY_TOKEN (sin el prefijo VITE_), que queda del lado servidor.
VITE_MONDAY_TOKEN=
